# Local machine paths (not committed)

Copy `paths.example.json` → `paths.json` and set absolute paths to the web repo on this machine.

Agents must read `paths.json` before assuming `../Serenova-F-t` layout.
The `.local/` folder is gitignored except this example (example is committed as `.local/paths.example.json` at repo root — see repo docs).
