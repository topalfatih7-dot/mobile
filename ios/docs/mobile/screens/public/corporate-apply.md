# Public — Corporate Apply (LOCK)

**MOBILE DIFF:** Uygulamada rota yok.

- Form → `POST /api/contact` `corporate_application`
- Captcha + rate limit
- Success/error messages from web page
- Captcha + rate limit
- Success/error messages from web page
