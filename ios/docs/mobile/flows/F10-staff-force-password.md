# F10 — Staff Force Password

1. Staff login with temp password  
2. `tempPasswordIssued` true → full-screen change password (current + new + confirm)  
3. Update auth password + clear `staff.data.tempPasswordIssued`  
4. Refresh → Staff shell unlocked  
