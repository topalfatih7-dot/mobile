# F11 — Coach Build & Send Program

1. Staff → Clients → member → Program  
2. Filter library → add to cart (`createCartEntry`: reps/duration, note, order)  
3. Reorder / remove; preview video (signed URL)  
4. Validate package date windows (`programPackageScope`)  
5. `CoachProgramSendModal` multi-step → `createProgram` for member  
6. Member sees program in Programs + Calendar  

Mobile UX: cart list + bottom sheet send; sticky actions.
