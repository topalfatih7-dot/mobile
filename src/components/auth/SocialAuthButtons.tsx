/**
 * MOBILE DIFF P1: sosyal giriş kapalı — bileşen render etmez.
 * Dosya layout slot için duruyor; ileride OAuth açılınca içeriği geri konur.
 */
export function SocialAuthButtons() {
  return null;
}
