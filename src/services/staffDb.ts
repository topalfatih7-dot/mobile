/**
 * Staff mutations — web supabaseDb staff surface (minimal port).
 */
export { createProgram, hydratePlatform } from '@/services/platformDb';
export { getStaffClients } from '@/utils/staffClients';
